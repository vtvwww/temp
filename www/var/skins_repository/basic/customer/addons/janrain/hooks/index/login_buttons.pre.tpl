{* $Id$ *}
{if $addons.janrain.appdomain}
<div class="janrain-wrap">
	<span>{$lang.or_use}</span>{include file="buttons/button.tpl" but_role="text" but_meta="janrainEngage nobg" but_text=$lang.social_login}
</div>
{/if}